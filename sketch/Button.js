class Button {
    constructor(label, x, y) {
        console.log('label: ', label)
        console.log('x: ', x)
        console.log('y: ', y)

        this.label = label;
        this.x = x;
        this.y = y;

        console.log(this)
    }
}